
@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" Content="width=device-width, initial-scale=1" />
    <title>订单调试</title>
    <link rel="stylesheet" href="~/plugins/zui/css/zui.min.css">
    <link href="~/plugins/zui/lib/uploader/zui.uploader.min.css" rel="stylesheet">
</head>
<body>
    调试结果 = @Model
    @using (Html.BeginForm("OrderPay", "LJSheng", FormMethod.Get, new { @class = "form-horizontal", enctype = "multipart/form-data" }))
    {
        <div class="form-group">
            <label class="col-sm-2">订单号</label>
            <div class="col-md-2 col-sm-2">
                <input type="text" name="OrderNo" value="" class="form-control">
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-2">网银号</label>
            <div class="col-md-3 col-sm-5 has-success">
                <input type="text" name="TradeNo" value="19840204" class="form-control" placeholder="必填项">
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-2">价格</label>
            <div class="col-md-3 col-sm-5">
                <input type="text" name="PayPrice" value="1000" class="form-control" placeholder="必填项,只能是数字" onkeyup="value=value.replace(/[^\d.]/g,'');">
            </div>
        </div>
        <div class="form-group">
            <label class="col-sm-2">支付类型</label>
            <div class="col-md-2 col-sm-2 has-success">
                <input type="text" name="PayType" value="1" class="form-control" placeholder="必填,只能是数字" onkeyup="value=value.replace(/[^\d]/g,'');">
            </div>
        </div>
        <div class="form-group">
            <div class="col-sm-offset-2 col-sm-3">
                <button class="btn btn-primary btn-lg btn-block" type="submit">提交</button>
            </div>
        </div>
    }
</body>
</html>
